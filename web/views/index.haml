.title
  .moviesList.withPosters.clearfix
    - @movies.each do |movie|
      .movie
        %h2= movie.name
        - if movie.thumbnail
          %img.poster{:src => movie.thumbnail.href}
        - else
          .noPoster
            No Image Available
